import { css } from '@emotion/react';

export const Loader = () => {
    return (
        <div
            css={css`
                margin: 20px auto;
            `}
        >
            ...Loading
        </div>
    );
};
